<div class="span12">

	<div class="span9  bookdate-box ">
		<legend>
  书单 > <%= link_to "所有标签", tags_path %></legend>
<div class="tags" >
	<% @tags.each do |tag| %>
	<% if tag.posts.count > 1 %>
		<%= link_to "#{tag.name}(#{tag.posts.count})", tag_post_path(tag), :class => "tag_all" %>
	<% end %>
	<% end %>
</div>
	</div>
</div>