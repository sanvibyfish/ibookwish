<%= render 'base' %>
<div class="tools">
  <%= link_to '修改', edit_cpanel_tag_path(@tag), :class => "btn btn-small btn-success" %>
  <%= link_to '返回', cpanel_tags_path, :class => "btn btn-small" %>
</div>
<div id="cpanel_tag">
    <p>
    <b>Name:</b>
    <%= @tag.name %>
  </p>
  </div>