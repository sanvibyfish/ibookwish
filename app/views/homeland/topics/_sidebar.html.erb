<div class="sidebar">

  <% if params[:action] == "index" %>
    <div class="bookdate-box">
      <h4>社区论坛</h4>
      <div class="content">
        <a class="btn btn-success" href="<%= new_topic_path %>">发布新帖</a>
         <a style="font-size:24px;line-height:2px;" href="<%= homeland.feed_topics_url %>" title="用RSS阅读器订阅论坛新贴" rel="tooltip">
          <i class="icon-rss", alt = "订阅论坛新贴"></i>
        </a>
      </div>
    </div>
  <% end %>

  <% if ["show","new","edit","node"].index(params[:action]) and !@node.blank? %>
    <div class="bookdate-box">
      <h4><a href="<%= node_topics_path(@node.id) %>"><%= @node.name %></a></h4>
      <% if ["new"].index params[:action] %>
      <p class="sumamry">
        <%= @node.summary %>
      </p>
      <% end %>
      <div class="content">
        <a class="btn btn-success" href="<%= new_topic_path(:node => @node.id) %>">发布新帖</a>
      </div>
    </div>
  <% end %>

  <% if ["new"].index(params[:action]) %>
    <div id="topic_new_tip" class="bookdate-box">
      <h2>发贴说明</h2>
      <ol>
        <li>发布后正文排版会自动安你在文本框里面输入的样子排列.如果里面带有 URL地址，将会自动转变为超级链接。</li>
      </ol>
    </div>
  <% end %>

  <% if params[:action] == "index" %>
    <div class="totals bookdate-box">
    	<h4>统计信息</h4>
    	<ul>
    		<li>贴子数: <%= Homeland::Topic.count %> 篇</li>
    		<li>回贴数: <%= Homeland::Reply.count %> 条</li>
    	</ul>
    </div>
  <% end %>
  
</div>
