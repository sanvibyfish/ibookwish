<div class="piece">
	<%= user_avatar_tag(comment.user, :style => "48x48", :class => "signature") %>
<div class="info_bar">
	<span class="name">
<%= link_to comment.user.name, user_path(comment.user) %>
		</span>
<%= distance_of_time_in_words(comment.created_at,Time.now) %>前

<div class="holder pull-right">
	<% #FIXME 目前只能@当前的人，是否要@帖子以外的人 %>
	<%= link_to "回复", "javascript:void(0)", :class => "rt", "data-user-name" => "#{comment.user.name}" %>
</div>

</div>
<div class="messages" id="1190">
<p>
<%=raw comment_body_tag(comment) %>
</p>

</div>
</div>